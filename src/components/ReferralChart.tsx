import React from 'react';

const ReferralChart: React.FC = () => {
  return (
    <div className="bg-green-100 h-32 flex items-center justify-center">
      Referral Chart Placeholder
    </div>
  );
};

export default ReferralChart;
